psql -h localhost -d myPrueba -U admin //COMANDO POR TERMINAL PARA CONECTAR A DB EN EL CONTENEDOR DE POSTGRES

const { Client } = require('pg');

const client = new Client({
    host: 'localhost',
    port: 5432,
    user: 'admin',
    password: 'admin',
    database: 'myPrueba'
});

client.connect();

client.query("SELECT * FROM public.tasks",(err, result) => {
    if(err){
        console.log(err);
    }else {
        console.log(result.rows);
    }
    client.end();
});